{
  "name": "weather-oracle-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for Weather Oracle",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service",
    "deploy-studio": "graph deploy --studio weather-oracle",
    "create-local": "graph create --node http://localhost:8020/ weather-oracle",
    "remove-local": "graph remove --node http://localhost:8020/ weather-oracle",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 weather-oracle"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
